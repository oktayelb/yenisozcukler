services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /data/sozluk.db
    volumes:
      - ${DATA_DIR:-./data}:/data:z
    restart: unless-stopped
